<% (FileManager.file_folder_list('public/uploads/Heroes_Weapons/')).each do |hero_name| %>
  <li class="nav-item">
    <a class="nav-link" href= <%= 

    if DataCleaner.clean_hero_name(hero_name).match(/\s/)
      "/hero/#{DataCleaner.clean_hero_name(hero_name)}".gsub(' ','%20')
    else
      "/hero/#{DataCleaner.clean_hero_name(hero_name)}"
    end

    %> ><%= DataCleaner.clean_hero_name(hero_name) %></a>
  </li>
<% end %>
